import { TrainingNamePipe } from './training-name.pipe';

describe('TrainingNamePipe', () => {
  it('create an instance', () => {
    const pipe = new TrainingNamePipe();
    expect(pipe).toBeTruthy();
  });
});
